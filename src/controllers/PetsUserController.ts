import { Handler } from 'express';

export class PetsUserController {
  // GET /users/:userId/pets
  getPets: Handler = async (req, res, next) => {};
  // POST /users/:userId/pets
  // PUT /users/:userId/pets/:petId
  // DELETE /users/:userId/pets/:petId
}
